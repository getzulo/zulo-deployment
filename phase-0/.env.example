# DNS provider credentials for Let's Encrypt DNS-01 (wildcard cert)
# Example: Cloudflare
CF_DNS_API_TOKEN=your_token_here
CF_ZONE_API_TOKEN=your_token_here

# Per-tenant JWT keys (control-plane will auto-generate in Phase 1)
JWT_KEY_T1=<base64-48-bytes>
